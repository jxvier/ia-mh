---
title: "Nombre Apellido"
rol: "Coordinación de la sección de IA"
orden: 1
---

Perfil de ejemplo. Para agregar una persona, crea un archivo `.md` en `src/equipo/` con `title` (nombre), `rol`, `orden` y opcionalmente `foto` (ruta a una imagen en `src/img/`).
